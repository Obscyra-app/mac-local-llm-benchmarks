# Hebrus v0.3.0 + Qwen3.6-35B-A3B на MacBook Air M3 16GB — замеры

**Дата:** 31 августа 2026 · **Цель:** большой офлайн-контекст (64K) для Hermes Agent

## Конфигурация

- **Движок:** Hebrus `v0.3.0`, commit `463eaa068d1a`, Metal/arm64 (форк antirez/ds4)
- **Модель:** Qwen3.6-35B-A3B-Hebrus-ExpertMajor-v2-MLX-Affine4-G64.gguf, 20.81 GB, SHA-256 сверен
- **Режим:** SSD expert streaming (единственный квалифицированный на 16GB: full residency
  потребовал бы 24.67 GiB против бюджета 11.84 GiB — отказал auto)
- **Запуск:** `hebrus-server -m <gguf> --ctx 65536 -n 256 --host 127.0.0.1 --port 8000`
- **Hermes:** профиль с окном 65 536 токенов, OpenAI-compatible API, tool calling работает
  (`tool_choice=auto`; принудительный named для Qwen3.6 не поддержан)

## Замеры (честные, живые прогоны)

| Сценарий | Результат |
|---|---|
| Генерация, ctx 2048 | 3.67 tok/s, zero swapout |
| Генерация, ctx 4096 (JSON) | 2.86 tok/s, zero swapout |
| Prefill 7 451 токенов (64K-сервер) | **177 tok/s** (42.0 с) |
| Prefill 7 894 токенов | 185 tok/s (42.7 с) |
| Генерация на 64K-контексте (чанки по 50) | 4.9–7.7 tok/s, avg **6.5 tok/s**, 412 токенов за 63 с |
| Non-thinking ответ (тривиальный вопрос) | ~3 с до полного ответа |
| Tool call (формат + парсинг) | корректный `todo` call, DSML-маркеры распознаны |

Разброс скорости чанков (4.9 → 7.7 tok/s внутри одной генерации) — SSD-стриминг экспертов:
часть экспертов попадает в кэш, часть читается с диска.

## Память — главная история

16 GiB unified: модель 19.37 GiB + контекст/KV 3.06 GiB + headroom 2.25 GiB = 24.67 GiB
потребность, поэтому **только SSD-стриминг**. Планировщик Hebrus «guarded»: при нехватке
отказывается кэшировать, а не свопит.

- **Размер кэша фиксируется при старте сервера** по свободной памяти в этот момент:
  утренний запуск (машина свободна) → кэш декода 1601 эксперт; вечерний (параллельные GUI)
  → 641. Разница 2.5×.
- **64K-запуск требует закрыть Codex/ChatGPT** (~2 GiB): при открытом — memory gate
  отказывает на reclaimable 4.93 GiB.
- **Задокументированный отказ:** на втором запросе вечером — `HTTP 500: failed to switch
  Qwen SSD cache to prefill phase`. Лог: slab admission `decision=deny proposed=541.69 MiB,
  host 3.02/2.92 GiB, Metal 6.55/11.84 GiB` → macro prefill отменён. Это штатная защита
  (отказ вместо swap), лечится освобождением памяти; сервер не падает, но запрос ретраится
  безуспешно, пока память не появится.
- Контроль: `Swapouts` в `vm_stat` — за все чистые прогоны остался неизменным (17500).

## Сравнение с раундом 29.08 (см. LOCAL-LLM-TESTING.md)

| | gpt-oss-20b MXFP4 (llama.cpp) | Qwen3.6-35B-A3B (Hebrus SSD) |
|---|---|---|
| Веса | 11 GB, full residency | 20.81 GB, SSD-стриминг |
| Генерация | **17–25 tok/s** | 2.9–6.5 tok/s |
| Prefill ~7.5K | — | 177–185 tok/s |
| Контекст | 8K практично (wired limit 13.8 GB) | **64K реально** |
| Tool calling | 2/2 | работает (auto) |

**Вывод:** gpt-oss-20b остаётся быстрым офлайн-драйвером; Hebrus+Qwen3.6 — единственный
способ получить настоящий 64K контекст офлайн на 16GB, ценой 3–4× меньшей скорости
генерации и строгой дисциплины памяти (поднимать сервер на свободной машине, закрывать
тяжёлые GUI). Prefill быстрый (177 tok/s) — повторные длинные промпты не больно.

## Методология

Замеры через `vm_stat`/`memory_pressure` до/после (рост Swapouts = прогон бракуется),
сервер поднимался непосредственно перед сессией. Ошибки не ретушировались: отказ
slab-admission задокументирован в исходном виде.
